/**
 * @brief Entry point for verifying zero-initialization of a structure.
 *
 * Initializes a vector structure to zero to confirm that zero-initialization is supported.
 *
 * @return int Always returns 0.
 */

int main()
{
  vector v = {0};
}
