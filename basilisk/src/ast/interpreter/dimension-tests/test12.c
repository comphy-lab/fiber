/**
 * @brief Initializes an array of 100 doubles, retrieves the last element, and displays its value.
 *
 * Sets each element of the array to a constant value, then retrieves the value at index 99 and passes it to the display function.
 *
 * @return int Exit status code.
 */
int main()
{
  double a[100];
  for (int i = 0; i < 100; i++)
    a[i] = 2. [1];

  int i1 = 99;
  double v = a[i1];
  display_value (v);
}
